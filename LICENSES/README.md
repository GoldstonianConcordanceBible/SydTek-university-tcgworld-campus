# Licensing Map (Code vs Content)

This repository may contain multiple asset types with different intended licenses.

## Default
- **Code**: MIT (see `/LICENSE`)
- **Documentation & datasets**: CC BY 4.0 (unless a subfolder specifies otherwise)

## Folder-level overrides
Any folder can include its own `LICENSE` file that overrides the default for that folder.